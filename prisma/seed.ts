export async function main(): Promise<void> {
  // Step B will populate real seed data from docs/SPEC.md.
  // Keep this script explicit so beginners can run it safely later.
  // eslint-disable-next-line no-console
  console.log("Seed placeholder: run Step B to generate catalog data.");
}

main().catch((error) => {
  // eslint-disable-next-line no-console
  console.error(error);
  process.exit(1);
});

