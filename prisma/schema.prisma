generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Step B will add full models and constraints from docs/DATA_MODEL.md.

